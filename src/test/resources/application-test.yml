spring:
  application:
    name: trustline


  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    clean-disabled: false


jwt:
  signin:
    key: adeleyeTrustline
    secret: secret

firebase:
  config:
    location: firebase-location

email:
  resend:
    api-key: resend-test-api-key

cloudinary:
  cloud-name: test-cloud
  api-key: test-api-key
  api-secret: test-api-secret
  mailerSend:
    api-key: mailer-send